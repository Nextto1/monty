#include "monty.h"
/**
 * f_stack - it prints out to the top
 * @head: the head stack for the function
 * @counter: the line_number for the function
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
