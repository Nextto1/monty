#include "monty.h"
/**
  *f_nop- it does nothing
  *@head: head stack
  *@counter: the line_number for the function
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
